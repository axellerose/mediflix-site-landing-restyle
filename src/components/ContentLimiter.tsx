import styled from "styled-components"
import Box from "./Box"

const ContentLimiter = styled(Box)`
  /* max-width: ${props => props.theme.breakpoints.xl}; */
  max-width: 2560px;
  margin: 0 auto;
`

export default ContentLimiter
